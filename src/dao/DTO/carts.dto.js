export default class CartManager{

    constructor(cart){
        this.products = [
            this.product = cart?.product ?? '',
            this.quantity = cart?.quantity ?? 0
        ] = cart?.products ?? []

    }

}